# Data

The notebook uses the public IBL/Neuromatch stimulus-aligned archive. Data are
downloaded on demand and are intentionally excluded from Git.

Set `OCTAGRAM_DATA_ROOT` to reuse an existing cache or mounted dataset.
