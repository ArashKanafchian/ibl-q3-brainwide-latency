# Results

`q3/tables/` and `q3/figures/` contain the final committed Q3 results.

New full-run outputs should be written to `q3/generated/`; expensive reusable
objects should be written to `q3/checkpoints/`. Both are excluded from Git.
